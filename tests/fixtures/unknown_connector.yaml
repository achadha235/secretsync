version: 1
changeDetection: always-write
secrets:
  a:
    env: A_ENV
sets:
  s:
    include: [a]
destinations:
  mystery:
    connector: not-a-real-connector
    auth:
      tokenEnv: TOKEN_ENV
deployments:
  - name: d1
    set: s
    destination: mystery
    scope: {}
    secrets:
      a: A
