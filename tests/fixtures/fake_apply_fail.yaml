version: 1
changeDetection: always-write
secrets:
  databaseUrl:
    env: YB_DATABASE_URL
sets:
  app:
    include: [databaseUrl]
destinations:
  sink:
    connector: fake-individual
deployments:
  - name: fail-one
    set: app
    destination: sink
    scope:
      env: test
    secrets:
      databaseUrl: FAIL_ME
